<!DOCTYPE html>
<html lang="fr">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>{% block title %}Mon Application{% endblock %}</title>
    <link rel="icon" href="data:image/svg+xml,<svg xmlns=%22http://www.w3.org/2000/svg%22 viewBox=%220 0 128 128%22><text y=%221.2em%22 font-size=%2296%22>⚫️</text><text y=%221.3em%22 x=%220.2em%22 font-size=%2276%22 fill=%22%23fff%22>sf</text></svg>">
    
    {# Bloc pour les feuilles de style CSS #}
    {% block stylesheets %}
        {# Exemple : Lien vers Bootstrap CSS #}
        <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css" rel="stylesheet">
    {% endblock %}

    {# Bloc pour les scripts JavaScript #}
    {% block javascripts %}
        {# Exemple : Lien vers Bootstrap JS #}
        <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/js/bootstrap.bundle.min.js"></script>
    {% endblock %}
</head>
<body>
    {# En-tête de la page #}
    <header class="bg-primary text-white text-center py-4">
        <h1>Mon Application</h1><br>
        <nav>
            <ul class="list-inline">
                <li class="list-inline-item"><a href="{{ path('app_pages') }}" class="text-white text-decoration-none">Accueil</a></li>
                <li class="list-inline-item"><a href="{{ path('app_connexion') }}" class="text-white text-decoration-none">connexion</a></li>
                <li class="list-inline-item"><a href="{{ path('app_inscription') }}" class="text-white text-decoration-none">inscription</a></li>
                <li class="list-inline-item"><a href="{{ path('app_about') }}" class="text-white text-decoration-none">À propos</a></li>
            </ul>
        </nav>
    </header>

    {# Bloc pour le contenu principal de la page #}
    <main class="container my-5">
        {% block body %}{% endblock %}
    </main>

    {# Pied de page #}
    <footer class="bg-dark text-white text-center py-3">
        <p>&copy; {{ 'now'|date('Y') }} Mon Application. Tous droits réservés.</p>
        <p>Contactez-nous : <a href="mailto:contact@monapp.com" class="text-white">ghofranmejaouel@gmail.com</a></p>
    </footer>
</body>
</html>